def fib(x):
    if ((x==0)|(x==1)):
        return x
    else:
        return fib(x-1)+fib(x-2)
x = int(input("Enter the number: "))
print("Here is the series:")
for i in range (0,x):
    print(fib(i))
